"use client";

export default function ServicesHero() {
  return (
    <section className="w-full min-h-screen flex items-center justify-center px-[6vw]">
      <h1 className="text-[clamp(3.5rem,7vw,7rem)] font-semibold text-center leading-[1.05] max-w-[1200px] text-[#2b2b2b]">
        We are a design studio that <br />
        loves to create together.
      </h1>
    </section>
  );
}
